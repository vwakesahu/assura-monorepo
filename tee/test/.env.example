# Ethereum Private Key for signing payments
# This test wallet needs to have USDC on Base Sepolia
# Generate with: uv run python -c "from eth_account import Account; acc = Account.create(); print(f'Address: {acc.address}\nPrivate Key: {acc.key.hex()}')"
PRIVATE_KEY=0x...

# API Configuration
API_URL=http://localhost:4021

# AI Provider Configuration (for reference - set in the service, not the client)
# AI_PROVIDER=ollama (default)
# AI_PROVIDER=gaia (to use Gaia Nodes)