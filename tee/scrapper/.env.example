# GitHub Personal Access Token
# Required for automatic PR creation
# Generate one at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=your_github_token_here

# Optional: Always create PR even if no new addresses found
# Set to 'true' to create PR on every poll, 'false' or unset to only create when new addresses are found
ALWAYS_CREATE_PR=false
