# x402 Payment Configuration
ADDRESS=0x492F9757240365621fA03fbcee80f3eA72b98d15
X402_NETWORK=base-sepolia
X402_PRICE=$0.001
X402_ENDPOINT_URL=http://localhost:4021/summarize-doc

# Ollama Configuration (default provider)
OLLAMA_HOST=http://localhost:11434
AI_PROVIDER=ollama

# Gaia Node Configuration (alternative provider)
# Uncomment and configure these to use Gaia instead of Ollama
# AI_PROVIDER=gaia
# GAIA_NODE_URL=https://your-node-id.gaia.domains/v1
# GAIA_MODEL_NAME=Qwen3-30B-A3B-Q5_K_M
# GAIA_API_KEY=your-gaia-api-key

# Agent0 SDK Configuration
AGENT0_CHAIN_ID=84532 # base seploia
AGENT0_RPC_URL=https://base-sepolia.g.alchemy.com/v2/your-api-key
AGENT0_PRIVATE_KEY=your-private-key-here
AGENT0_IPFS_PROVIDER=pinata
AGENT0_PINATA_JWT=your-pinata-jwt-token

# Agent Configuration
AGENT_NAME=Oasis ROFL x402 Summarization Agent
AGENT_DESCRIPTION=Oasis ROFL x402-enabled document processing agent running in TEE. REST API for async summarization. Multi-provider AI backend (Ollama/Gaia). On-chain registered with reputation trust model.
AGENT_IMAGE=http://localhost:4021/logo.png
AGENT_WALLET_ADDRESS=

# Oasis ROFL Configuration
ENVIRONMENT=development  # Set to 'production' to enable TEE signing and Oasis ROFL metadata storage
DEBUG_SIGNING=false  # Set to 'true' to use mock signing keys for testing

# Namespace.ninja ENS Configuration
NAMESPACE_API_KEY=your-namespace-api-key-here
NAMESPACE_NETWORK=mainnet  # 'mainnet' or 'sepolia' for testing
